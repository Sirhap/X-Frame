# MCP 已迁出

本仓库不再包含 XSXB MCP 实现。

要看或改 MCP（`xsxb_list_projects`、抠图、种脚、place、回执 v2 等），请到：

**https://github.com/Sirhap/x-frame-mcp**

本地目录通常是 `/Volumes/other/IdeaProjects/sirhao/x-frame-mcp`。Cursor 的 `args` 指向该仓的 `mcp/xsxb_mcp_server.js`。读写本 Tuner 的项目和帧时设：

```text
XSXB_ROOT=<本仓库根目录>
XSXB_TUNER_ROOT=<本仓库根目录>
```
