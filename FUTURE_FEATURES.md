# 可添加的功能建议

当前项目已覆盖帧动画调参、批量抠图、Godot同步等核心。以下是可添加的补充功能（优先级从高到低）：

1. **AI智能建议** - 批量抠图/调参时提供AI生成建议（使用内置LLM或本地模型）。
2. **多引擎导出** - 支持Unity/Unreal/UE5导出模板。
3. **协作共享** - 项目版本控制 + 多人实时调参（WebRTC）。
4. **移动/AR支持** - PWA + AR预览模式。
5. **自动化模板生成** - Godot 4.3+ 运行时模板一键生成。
6. **3D模型集成** - 直接导入FBX/GLB模型转为帧动画。
7. **AI宠物图集** - 扩展Codex Pets到3D模型。
8. **性能模式** - 移动端/低配设备优化（减 bundle、WASM降级）。

建议先实现1和2作为MVP。