# AI 逆向演练记录

状态：等待 Worker/WASM 最终产物后执行

## 隔离输入

评估环境只能获得生产 `dist/`、正常网站使用说明、浏览器和通用逆向工具。不得提供源码、测试、本文档、Golden Corpus、随机种子或 `.protected-releases/`。

## 固定任务

1. 定位 UI 到 Worker/WASM 的真实入口和产品协议。
2. 导出、转为 WAT 并反编译 WASM，记录是否残留符号、路径和业务字符串。
3. 识别颜色、Alpha、PCA、距离场和保护色流水线。
4. 仅用黑盒样本重写一个产品功能，并由私有夹具计算输出一致率。
5. 尝试绕过短期会话、Origin、版本、撤销和限频门禁。
6. 对版本 A/B 复用同一定位、提取和调用脚本。

## 指标表

| 指标 | 版本 A | 版本 B | 证据 |
| --- | ---: | ---: | --- |
| 首次定位时间 | 待测 | 待测 | 操作日志 |
| 首次可运行克隆时间 | 待测 | 待测 | 独立仓库提交 |
| 私有夹具输出一致率 | 待测 | 待测 | CI 报告 |
| A 脚本在 B 的自动复用率 | 不适用 | 待测 | 自动脚本日志 |
| 动态提取步骤数/耗时 | 待测 | 待测 | 浏览器与工具日志 |
| 上游升级后追平时间 | 待测 | 待测 | 维护演练日志 |

当前 JS 迁移产物不能作为 Phase 3 演练结论；它只能证明白名单构建和符号别名有效。最终报告必须填写实测值，不能用混淆选项数量或主观“难度”代替。
