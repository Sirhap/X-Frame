interface:
  display_name: "X-Frame"
  short_description: "Process, tune, and wire Godot frame animations."
  default_prompt: "Use $x-frame to process these local animation assets, tune them, and fully wire the result into my Godot game."
