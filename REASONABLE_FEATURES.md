# 合理可添加的功能（已排除AI/AR/协作/多引擎）

当前项目核心已覆盖帧调参、抠图、Godot同步。以下是合理补充（优先级高到低）：

1. **项目Git集成** - 动画组/调参/素材版本控制 + 一键回滚。
2. **脚本化API** - Agent CLI / HTTP API，纯脚本化批量操作。
3. **自定义Godot模板** - 用户自定义运行时模板（res://xsxb_frame_tuner/），一键生成。
4. **批量项目导出** - 完整Godot项目ZIP导出（含所有runtime）。
5. **动画曲线编辑器** - 帧时长/播放节奏曲线编辑 + 关键帧。
6. **实时性能监控** - 浏览器实时FPS/内存监控 + 优化建议。
7. **错误诊断工具** - 自动检查框体/挂件/音频接线 + 修复建议。
8. **自动帧预测** - 基于历史帧预测下一帧参数（PCA/相似度）。

优先级：1、3、4、7。

下一步：实现Git集成或脚本化API。